# Variables de entorno — PRODUCTION
# Cargadas por docker-compose.yml en este mismo directorio.
# Mantén este archivo libre de secretos; usa un secret manager para credenciales reales.

NODE_ENV=production

# Puerto donde escucha el server SSR de Express (host:container)
PORT=4000
