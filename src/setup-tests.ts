// El runner @angular/build:unit-test inicializa el TestBed automáticamente
// (via `init-testbed.js`), por lo que aquí sólo añadimos polyfills o setup
// global adicional que requieran los tests.
export {};
